# =====================================================================
# Predicción de Cáncer — UAX Ingeniería Matemática 2025/2026
#
# Entorno: conda env `uax-tf` (Python 3.12 + TensorFlow 2.x).
#     conda activate uax-tf
#
# Instalación preferida vía conda-forge:
#     conda install -c conda-forge <pkg>
# Fallback con pip dentro del entorno:
#     pip install -r requirements.txt
#
# Versiones fijadas (==) en paquetes críticos para reproducibilidad,
# con margen (>=) en utilidades.
# =====================================================================

# Núcleo
numpy==1.26.4
pandas==2.2.2
scipy>=1.11

# Machine Learning clásico
scikit-learn==1.5.1
xgboost>=2.0.3
joblib>=1.3.2

# Red neuronal (MLP)
tensorflow>=2.16.2

# Visualización
matplotlib>=3.8
seaborn>=0.13

# ETL desde Azure (vía DataSource del IDE en notebooks)
azure-identity>=1.16
# pyodbc>=5.1     # Activar si se implementa ETL programático en src/
# sqlalchemy>=2.0

# Web app (dashboard FastAPI + Jinja2; Plotly.js se carga en el cliente)
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
pydantic>=2.7

# Entregable
python-pptx>=0.6.23

# Notebooks y EDA
jupyter>=1.0
jupyterlab>=4.2
ipykernel>=6.29
ipywidgets>=8.1
tqdm>=4.66

# Calidad de código y tests
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10

# Variables de entorno
python-dotenv>=1.0
